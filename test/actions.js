import sinon from 'sinon'
import {expect} from 'chai'
import actions from '../app/actions'


describe("login action", () => {

    it('should work if valid identity and password', () => {

    })

    it('should call a failure if invalid identity and password', () => {

    })

    it('should call a failure if empty identity or password', () => {

    })

 })
