import React from 'react';
import Router from 'react-router';

const Route = Router.Route,
      RouteHandler = Router.RouteHandler,
      DefaultRoute = Router.DefaultRoute;

export default (
    <Route>
        <DefaultRoute name="popular" />
    </Route>
);

